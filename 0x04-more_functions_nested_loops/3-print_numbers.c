#include <main.h>
/**
 * print_numbers - checks prints the numbers, from 0 to 9
 * followed a new line
 *
 * Return: void
 */
void print_numbers(void)
{
	for (n = 0, n < 10, n++)
		_putchar (n);
	_putchar ('\n');
}
