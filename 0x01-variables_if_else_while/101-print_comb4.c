#include <stdio.h>

/**
 * main - Program entry point
 *
 * Return: 0 success. Error code otherwise
 */
int main(void)
{

	int n, e, a;


	for (n = 48; n < 58; n++)
	{
		for (n = 49; n < 58; n++)
        {
	       	for (a = 50 ; a < 58 ; a++)
                {
			if (a > e &&
                        putchar(e);
                        putchar(n);
                        if (n == 57 && e == 56)
                        {
                                break;
                        }
                        putchar(',');
                        putchar(' ');
                }
                n++;
        }
        e++;
}
printf("\n");
return (0);
}
