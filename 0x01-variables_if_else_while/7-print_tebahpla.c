#include <stdio.h>
/**
 *main - Program entry point
 *
 *Return: 0 on success. Error code otherwise
 */
int main(void)
{
	char m;

	for (m = 'z' ; m >= 'a' ; --m)
	{
		putchar(m);
	}
		putchar('\n');

		return (0);
}
